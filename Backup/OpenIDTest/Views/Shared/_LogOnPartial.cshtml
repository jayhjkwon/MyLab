@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Log Off", "Logout", "Authentication") ]</text>
}
else {
    @:[ @Html.ActionLink("Log On", "Login", "Authentication") ]
}
